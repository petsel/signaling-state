module.exports = function (wallaby) {
  return {
    files: [
      'src/**/*.js'
    ],
    tests: [
      'test/**/*.test.js'
    ],
    compilers: {
      '**/*.js': wallaby.compilers.babel()
    },
    trace: true,
    debug: true,
    env: {
      type: 'node',
      runner: 'node',
      params: {
        runner: '-r',
      },
    },
    testFramework: 'jest'
  };
};
